This script implements an object-following robot controller that uses real-time vision detections 
to automatically move toward a detected object, stop when it’s close, and prepare the robot’s arm and gripper for grasping.
It combines computer vision, mobile base control, and arm coordination within a single ROS node.
