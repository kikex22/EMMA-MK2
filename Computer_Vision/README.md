this codes is the second version of a real-time ROS visualization node for DarkHelp object detection.
It receives compressed camera frames and detection bounding boxes, overlays them on the image, 
  displays the results live with FPS information, and republishes both the annotated video stream 
  and detection metadata for downstream processing or monitoring.
